# 图像 FPGA · 六个月计划

> **最终目的** 2027 年 2 月底拿到图像 FPGA 岗 offer，目标 20-35K。
> **唯一标准** 手上那个项目能不能讲透 40 分钟 · 有没有量化证据 · 有没有上板实物。
> **节奏** 工作日 2h / 休息日 3h。每周只看本周。卡住超 20 分钟就问。
> **每日固定** HDLBits 1 题（加班可砍）。

## ★ ISP 分域（面试口径，背这句）

> 「我的 ISP 分三段：**RAW 域**做黑电平、坏点、白平衡增益——WB 放去马赛克**前**（Bayer 域每像素只乘一次、AWB 统计按相位分组天然在这、先平衡再插值伪彩少，但 WB 增益应用级在链路里最后实现，前期 1.0 直通占位）；**去马赛克后在线性 RGB 域**做降噪和 CCM——噪声在这可建模、CCM 会放大噪声所以降噪在前；**Gamma 之后进感知域**做锐化。」

映射到全链（**WB 在 demosaic 前**——工业主流；对角阵 WB 必须在满阵 CCM 之前，WB·CCM ≠ CCM·WB）：

| 域 | 模块 | 为什么在这一域 |
|---|---|---|
| **RAW 域** | BLC → DPC → AWB（统计+增益） | 坏点是像元级的，必须去马赛克前去掉；黑电平先扣掉判定阈值才有意义；WB 是通道增益，Bayer 域每像素 1 次乘法（RGB 域要 3 次）、AWB 统计按 R/Gr/Gb/B 相位分组天然在此 |
| **线性 RGB 域** | 去马赛克 → 降噪 → CCM | 噪声在这一域接近高斯、可建模；CCM 混色矩阵会把噪声放大串到别的通道，所以降噪放在它**之前**（原计划 AWB 在此，已修正到 RAW 域） |
| **感知域** | Gamma → 锐化 | Gamma 后是显示域的感知量，锐化在这里才符合视觉；但**噪声也在这被放大**，所以降噪必须在 Gamma 前 |

---

## 一、路线图

| 阶段 | 时间 | 内容 | 里程碑 |
|---|---|---|---|
| **P1** | 09 月（执行 month2-plan） | W3 收尾（去马赛克 + DPC）→ W4（BLC/白平衡/Gamma + 五级串联） | ISP 前端串联跑通 |
| **P2** | 10 月 | 降噪 RTL → USM RTL → CCM RTL → 3A 统计 + 评测收尾 | 四个模块 RTL 跑通 |
| **P3** | 11 月 | 全链预演 → 上板出图 → 帧存 + 配寄存器 + 闭环 | ★ 上板出图 |
| **P4** | 12 月 | 项目定稿 → 八股 → 模拟面试 | 简历定稿 |
| **投递** | 01–02 月 | 投递 + 面试 | offer |

**主线只留 5 项**，其余都是顺手做或机动项：**上板出图 · 三张表 · 画质评测 · 10 月四模块的 RTL（降噪 / USM / CCM / 3A 统计）· 软硬件协同闭环**。

**三张表**（11 月填实测值，简历硬通货）：资源表 / 时序表 / 带宽表。

---

## 二、P1 收尾 · 09 月

> **9 月按 `fpga-image-month2-plan.md` 执行，本文件不重复排期。**
> 当前位置：**W3（去马赛克 + DPC）**，之后还有 W4。

- **W3 收尾**：去马赛克 + DPC 跑通，把两者接成 `DPC → 去马赛克` 两级，**共用同一份 4 行行缓存**
- **W4**：BLC → **AWB 增益（Bayer 域，可先 1.0 直通占位）** → Gamma → **五级串联**（`BLC → DPC → AWB → 去马赛克 → Gamma`）
- **W4 加一项**：整理 **逐级延迟拍数表**（10 月全链和 11 月上板的地基，现在建好后面省一半时间）
- ⚠️ **W3/W4 执行时注意 DPC 的三条修正**（详见 `fpga-image-month2-W3-DPC-笔记.md`）：
  1. 判定用 **min/max 包络**，不要用"与邻域中值比较"（实测误伤 25.3%）
  2. 包络法**不需要排序网络**，6~14 个比较器即可
  3. **5×5 窗口的真正来源是 DPC 的 R/B 同色邻居**，去马赛克本身 3×3 就够

**✓ 9 月验收**：五模块串联跑通 · 延迟表齐了 · 能讲"ISP 前端每一步干嘛、为什么是这个顺序"

---

## 三、P2 · 10 月

> **本月目标：4 个新模块全部落到 RTL**（不是只画伪代码）。
> **画质评测不单占一周** —— 改成「每做完一个模块，就补该模块的指标脚本」，边做边建。

### W1 · ★★★ 降噪（双边滤波）· 含 RTL

- [ ] **理论**：噪声分类（散粒 / 读出 / 固定模式）· 高斯能降噪但会糊边
- [ ] **理论**：双边 = 空间核 × 值域核 —— 值域核让"和中心差得远的邻居"权重归零，所以能保边
- [ ] **理论**：**FPGA 为什么不直接算 exp()** —— 空间核做常数、值域核查 LUT（高频面试题）
- [ ] **上手**：MATLAB 跑通双边，对比高斯看保边效果
- [ ] **上手**：**RTL 实现** —— 3×3 窗口复用行缓存，值域权重查 LUT，加权求和 + 定点化 + 限幅
- [ ] **上手**：建 **PSNR / SSIM 脚本**（逐级对比的地基，后面每个模块都复用）
- [ ] **了解即可**：3D 降噪（时域）思路（不实现）
- [ ] **✓ 验收**：双边 RTL 仿真跑通 · 能讲"为什么保边" · "值域核怎么实现" · "和高斯的资源差异"

### W2 · ★★ USM 锐化 · 含 RTL

- [ ] **理论**：USM 公式 `锐化 = 原图 + α·(原图 − 高斯模糊)` · **halo 白边怎么来、怎么用阈值 / 边缘掩蔽压**
- [ ] **上手**：USM **RTL** —— 复用 8 月的高斯模块做模糊支路，加减法器 + α 定点缩放，halo 阈值可配
- [ ] **上手**：建 **MTF / SFR 脚本**（斜边法，量"锐化到底拉回多少分辨率"）
- [ ] **✓ 验收**：USM RTL 仿真跑通 · 能讲"怎么用已有模块搭出来" · "halo 怎么压" · 能报出锐化前后的 MTF

### W3 · ★★★ CCM · 含 RTL

- [ ] **理论**：CCM 是把 sensor 光谱响应校正到标准色空间（3×3 满矩阵）
- [ ] **理论**：**白平衡 vs CCM**（面试高频坑）—— 增益（对角矩阵）修色温 / 混色（满矩阵）修串色
- [ ] **上手**：CCM **RTL** —— 9 个乘法 + 加树 + 定点化 + 限幅（复用 CSC 的定点化经验）
- [ ] **上手**：建 **ΔE 脚本**，用 24 色卡验证 CCM 前后的色差改善
- [ ] **✓ 验收**：CCM RTL 仿真跑通 · **能讲清"白平衡和 CCM 的区别"** · 能报出 ΔE 改善

### W4 · 3A 统计 + 评测收尾（留 2~3 天缓冲）

- [ ] **上手（1.5 天）**：3A 统计模块 —— 复用直方图的 ping-pong 架构，只做统计 + 寄存器暴露，**不做闭环**
- [ ] **理论**：**3A 的软硬件分工边界** —— PL 只做帧级统计和逐像素应用，决策在控制端
- [ ] **理论**：**伪影识别清单** —— 拉链/伪彩 → 去马赛克 · 摩尔纹 → 采样 · halo → 锐化 · 涂抹感 → 降噪过强 · 坏点残留 → DPC 漏检
- [ ] **产出**：测试图卡准备（灰阶卡 / 24 色卡 / 分辨率卡）
- [ ] **⏳ 缓冲（2~3 天）**：补 W1~W3 没做完的模块（USM / CCM 的 RTL 最容易超期）
- [ ] **✓ 验收**：能讲 3A 分工 · **看到伪影能判断是哪一级引入的** · 图卡就位

---

## 四、P3 · 11 月

> **本月取舍**：「全链预演」放 W1，上板压到 W2 一周。若上板卡住，从 W3 的帧存借时间（帧存可退到 12 月或只讲方案）。

### W1 · ★★★ 全链 8 级集成预演（仿真）

> 上板前最后一道仿真关卡，把"链路搭不起来"的风险挡在板子外面。

- [ ] **上手**：**全链 8 级串联（仿真跑通）** `BLC → DPC → AWB → 去马赛克 → 降噪 → CCM → Gamma → 锐化`（为什么是这个顺序 → 见开篇「ISP 分域」；AWB 在去马赛克前的 Bayer 域）
- [ ] **上手**：**逐级延迟对齐表**（8 级最容易在这里翻车）
- [ ] **上手**：用评测脚本逐级对比，确认每级输出符合预期
- [ ] **上手**：提前摸清采集通路的信号 —— 像素时钟域 / 行场同步 / 数据有效，**确定流水线从哪个信号接进去**
- [ ] **产出**：三张表初稿
- [ ] **✓ 验收**：★ 全链 8 级仿真跑通 · 延迟对齐表齐了 · 三张表有初稿 · 知道流水线在哪接入

### W2 · ★ 上板出图（里程碑）

- [ ] **上手**：先确认采集通路给到的是 **RAW Bayer**（不是 YUV / RGB），不是就切成 RAW 输出
- [ ] **上手**：`采集 → ISP 全链入口 → HDMI 输出`，用 W1 的延迟表逐级对齐 valid
- [ ] **上手**：★ **上板看到真实图像**
- [ ] **调参**：逐个模块开/关，看图像和指标变化
- [ ] **⏱ 砍**：调参遍数（先把图出出来最重要）
- [ ] **✓ 验收**：★ 上板出图 · 能讲整条链路 `Sensor → 采集 → ISP → 帧存 → HDMI` · 能讲"8 级串联怎么对齐延迟"

### W3 · 帧存 + 控制端配寄存器

- [ ] **上手**：**片上帧存 ping-pong** —— 片上 URAM 放得下 1 帧 1080p RGB888，配 BRAM 做 2~3 帧缓存；省掉 DDR 控制器，延迟低且确定
- [ ] **可选**：DDR 帧存 ping-pong —— 复用 VDMA + GenLock 经验，满足 JD 的"DDR 帧存设计"要求（时间不够只讲方案）
- [ ] **上手**：控制端通过 AXI-Lite 配 PL 寄存器 —— 分辨率 / 各模块阈值 / 增益 / bypass 位（复用 MIPI 工程的 AXI-Lite 经验）
- [ ] **✓ 验收**：帧存跑通 · 控制端能配 PL 参数

### W4 · ★ AE/AWB 闭环 + 带宽时序 + 三张表实测

- [ ] **★ 上手**：**极简 AE/AWB 闭环** —— PL 暴露统计 → 控制端裸机 C 算增益/曝光回写 → 下一帧生效。命中 JD 的"Vitis 裸机协同"
- [ ] **理论**：**带宽预算** —— 按自己链路的口径算一遍，并能说出"什么情况下这个结论会失效"
- [ ] **理论**：**时序收敛** —— 像素时钟域 vs AXI 时钟域 · 多周期路径 · 流水线插入 · 关键路径定位
- [ ] **理论**：**视频流反压** —— valid/ready 握手 · 行首行尾处理 · FIFO 深度怎么算
- [ ] **产出**：三张表补成实测值
- [ ] **⏱ 砍**：AE/AWB 闭环（退回"只讲分工边界"）→ DDR 版本（退回只讲方案）
- [ ] **✓ 验收**：★ 闭环生效 · 能背出带宽预算 · 三张表是实测值

---

## 五、P4 · 12 月

| 周 | 做什么 |
|---|---|
| **W1** | **项目定稿**：ISP 全链写成简历主项目（背景 + 我做了什么 + 量化结果 + 难点）· 4 个接口项目包装成"视频通路集成经验" · 答辩材料 · 准备**三个"我踩过的坑"故事** |
| **W2** | **八股一轮**：图像算法 / FPGA 基础（CDC、时序分析、FIFO 深度、流水线平衡）/ 接口与系统（MIPI CSI-2、AXI、URAM vs BRAM、DDR 帧存、3A 分工）。每讲一个模块必须报出「资源 + 延迟 + 带宽」 |
| **W3** | **项目深挖 + 模拟面试**：按"项目 → 原理 → 实现 → 工程判断"四层追问，40 分钟一轮。重点练反向问题：做不到什么 / 什么场景该关掉 / 优化从哪下手 |
| **W4** | **八股二轮**（只过错题）+ 简历定稿 + 目标公司清单 + 准备向面试官提问的 3 个问题 |

**✓ 12 月验收**：简历定稿 · 能连续讲 40 分钟项目不被问倒 · 八股高频覆盖 · 三个踩坑故事能讲

---

## 六、投递与面试 · 01–02 月

| 时间 | 动作 |
|---|---|
| **01 上** | 集中投递，每周 10+ 家：医疗影像 · 半导体检测 · 车载 ISP · 安防显示 · 工业视觉 |
| **01 下** | 面试冲刺 + 每场面试后复盘 |
| **02** | 补投 + 谈薪（目标 20-35K） |

无锡及周边：半导体（华虹、卓胜微）与本地工业视觉、医疗设备厂优先网申。

---

## 七、机动项池

> 主线没跑完之前不碰。跑完按顺序挑。

| 优先级 | 项目 | 成本 |
|---|---|---|
| ★★★ | Canny 完整实现（滞后阈值在硬件上难做，极好的架构讨论题） | 1 周 |
| ★★★ | HLS / CNN 算子加速（卷积 / 池化 / 激活） | 2 周 |
| ★★☆ | 高阶去马赛克（边缘感知 / Hamilton-Adams） | 1 周 |
| ★★☆ | CLAHE（医疗岗才值得做） | 1 周 |
| ★☆☆ | 形态学腐蚀/膨胀 | 2 天 |

**行业倾向**：延后到 12 月第一周再定 —— 那时有实测数据和回复率参考。定完只在机动项池加 1 项，不动主线。

---

## 八、资源

| 用途 | 来源 |
|---|---|
| 行缓存 / CSC / 缩放 / 滤波 / 边缘 | 牟新刚《基于 FPGA 的数字图像处理原理及应用》 |
| 算法字典 · 降噪 · 色彩 | 冈萨雷斯《数字图像处理》第 3、5、6 章 |
| 降噪 / AWB / AEC / CCM / DPC 的 Python 参考实现 | GitHub 搜 `openISP` |
| 画质评测（MTF/SFR · ΔE · 图卡用法） | Imatest 官方文档 |
| 时序收敛 / CDC 规范 | Xilinx UG949 |
| 现成可复用模块 | 自己的行缓存 / 中值 / CSC 定点化模板 / ping-pong 架构 |

---

## 九、砍单顺序（时间不够时严格从上往下砍）

```
1 HDLBits → 2 3A 统计模块 → 3 AE/AWB 闭环 → 4 伪彩 / 高阶去马赛克深化 → 5 USM 的 RTL → 6 CCM 的 RTL

🚫 绝不砍：降噪 RTL · 上板出图 · 全链集成预演 · 三张表 · 画质评测脚本 · 项目定稿
```

**要整块砍，不要均匀降配。**

---

## 十、三条救命规则

1. **别等"状态好"才开始** —— 打开 Vivado 点一下就算开始，写一行 assign 就算进步。
2. **每周只看本周清单** —— 别翻到 11 月想"上不了板怎么办"，别翻到 12 月想"八股背不完怎么办"。做到就打勾。
3. **卡住超 20 分钟就问 AI** —— 双边的值域核、CCM 和白平衡的区别、ping-pong 第一帧边界、8 级延迟对齐、带宽推导，这些都能画图讲透。别一个人憋。
